Before using this code you have to install some module like mysql-connecter,custom tkinter

CMD Commands : pip install mysql-connector-python

	     	        pip install customtkinter




And you need a MySQL server like xampp,wamp,mysql bench etc.

After installing any SQL server create a database named quiz

By This command : create database quiz;



Create two table first login_info and second stu_info 



Create Teacher Table By Using This Command: create table login_info(Name varchar(30),Password varchar(50));

Create Student Table By Using This Command :create table stu_info(Name varchar(40),class varchat(2), Roll_No varchar(10), Section varchar(2),Gender varchar(10),Score varchar(10));
